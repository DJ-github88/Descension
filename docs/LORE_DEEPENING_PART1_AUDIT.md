# Mythrill Lore Deepening — PART 1: CONSISTENCY AUDIT (Findings)

> **Status:** Generated by programmatic cross-check of the live data (20 class files, `loreDictionary.js`, `backgroundData.js`, `npcStore.js`, `factionStore.js`, `zoneData.js`, `deepLocationData.js`, `locationCoordinates.js`).
> **Method:** Node harness loaded every module and verified each rule at the field level (normalization-aware: zones use hyphens `greymark-keep`, lore termIds use underscores `greymark_keep`; cross-refs convert between the two).
> **Initial totals:** 40 breaches — P0: 7 · P1: 24 · P2: 9. Three checks (1.1, 1.2, 1.6) clean.
> **Post-fix totals (all 7 P0 applied + verified by harness re-run):** **32 breaches — P0: 0 · P1: 22 · P2: 10.** Check **1.4 is now fully clean** (8/8 resolved). See "P0 Resolution Log" at the bottom.

---

## Checks that PASSED (no findings)

| Check | Result |
|---|---|
| **1.1** restriction ↔ subraceVariants parity | ✅ All 20 classes: `keys(subraceVariants)` === `restrictions.allowedSubraces`. No missing/orphan variants. |
| **1.2** headquarters.locationId existence | ✅ All 20 `livingOrder.headquarters.locationId` resolve to a real zone/deep-location/region/lore term. |
| **1.6** loreDictionary.secondaryRegions validity | ✅ All `secondaryRegions` entries are real region ids. (Semantic ↔ founder pairing is healthy; deferred to Part 2 native-weaving.) |

---

## 1.3 — currentLeader ↔ npcStore

**BREACH #1** — 1.3 currentLeader name does not match npcStore entry
- **File:field:** `classes/wardenData.js:164` — `livingOrder.currentLeader.name='Chain-Lord Alaric'`
- **Contradiction:** Warden's leader is `'Chain-Lord Alaric'`; the matching NPC is `npcStore['alaric']` = **'Alaric the Law-Keeper'** (title 'Chain-Lord of the Bound'). The other 19 leaders match their NPCs exactly AND each NPC's `locationIds` includes the class HQ. Only Warden's name string disagrees — which will break the Part 3 "clickable leader NPC link."
- **Severity:** P1
- **Fix:** Reconcile the two name strings to one canonical form (recommend the NPC name `'Alaric the Law-Keeper'` in the class file, with `'Chain-Lord'` kept only in `title`).

---

## 1.4 — background classHooks legality + classId resolution

> Root cause for the six `warden` findings below is a single id-casing bug: **`wardenData.js:149` is `id : "Warden"`** (capitalized, stray space before colon). Every other class uses lowercase `id: "<name>"`. `backgroundData.js` correctly writes `classId: 'warden'`, so the references never resolve.

**BREACH #2** — 1.4 classHooks.classId does not resolve
- **File:field:** `backgroundData.js:296` — `sumpsVeteran.classHooks.classId='warden'`
- **Contradiction:** `'warden'` resolves to no class (the Warden class `id` is `"Warden"`).
- **Severity:** P0 · **Fix:** Canonicalize `wardenData.js:149` to `id: "warden"` (lowercase) to match the other 19 classes and all background references.

**BREACH #3** — 1.4 classHook bridges impossible subrace combo
- **File:field:** `backgroundData.js:396` — `frostChanter.classHooks->harbinger`
- **Contradiction:** `frostChanter` allowedSubraces=`[skald_human]` ∩ `harbinger.allowedSubraces=[sylen_astril, muren_astril, solvarn_human, tessen_human]` = **∅**. A Skald-only background cannot bridge to a class that hard-blocks Skald.
- **Severity:** P0 · **Fix:** Re-point the hook to a class a Skald can take (e.g. `augur`/`berserker`/`warden`), or widen `frostChanter.allowedSubraces`.

**BREACH #4** — 1.4 classHooks.classId does not resolve
- **File:field:** `backgroundData.js:438` — `forgeWright.classHooks.classId='warden'`
- **Contradiction:** Same root cause as #2.
- **Severity:** P0 · **Fix:** Resolve via the #2 id fix.

**BREACH #5** — 1.4 classHooks.classId does not resolve
- **File:field:** `backgroundData.js:529` — `peakTracker.classHooks.classId='warden'`
- **Severity:** P0 · **Fix:** Resolve via the #2 id fix.

**BREACH #6** — 1.4 classHook bridges impossible subrace combo
- **File:field:** `backgroundData.js:586` — `merrowSailor.classHooks->apex`
- **Contradiction:** `merrowSailor` allowedSubraces=`[merryn_human, breaker_myrathil, deep_myrathil, river_myrathil]` ∩ `apex.allowedSubraces=[maskborne_mimir, mistwoven_mimir, unwoven_mimir, skald_human, clean_vreken, marked_vreken, ordan_human]` = **∅**. No Merryn/Myrathil can be an Apex.
- **Severity:** P0 · **Fix:** Re-point the hook to a class a Merryn/Myrathil can take (e.g. `gambit`/`minstrel`), or drop the hook.

**BREACH #7** — 1.4 tensionPairings.classId does not resolve
- **File:field:** `backgroundData.js:589` — `merrowSailor.tensionPairings.classId='warden'`
- **Severity:** P1 · **Fix:** Resolve via the #2 id fix.

**BREACH #8** — 1.4 classHooks.classId does not resolve
- **File:field:** `backgroundData.js:876` — `vaultScholar.classHooks.classId='warden'`
- **Severity:** P0 · **Fix:** Resolve via the #2 id fix.

**BREACH #9** — 1.4 tensionPairings.classId does not resolve
- **File:field:** `backgroundData.js:1023` — `deepCurrentGuide.tensionPairings.classId='warden'`
- **Severity:** P1 · **Fix:** Resolve via the #2 id fix.

> **Note on universal backgrounds:** `bloodlineHeir`, `hushSurvivor`, `monolithHunter`, `forgeWright`, `vaultScholar` carry empty `restrictions.allowedSubraces` by design (intentionally universal, per audit 7.3). These were **not** flagged for empty intersection — only their unresolved `warden` hooks are breaches.

---

## 1.5 — worldFriction region/location validity

**BREACH #10** — 1.5 Non-standard worldFriction generalization tokens
- **File:field:** `classes/*Data.js` — `worldFriction.region`
- **Contradiction:** Generalizations in use: `everywhere-else`, `human-settlements` (canonical) **plus** `everywhere` (spellguard) and `most-settlements` (toxicologist). All actual region refs (`bryngloom-forest`, `sundale`, `nordhalla`, …) are valid.
- **Severity:** P2 · **Fix:** Standardize on the agreed vocabulary (`everywhere-else`, `human-settlements`) so the Part 3 status-badge renderer can bucket them uniformly.

---

## 1.6 — (clean; see Passed table)

## 1.7 — relatedTerms bidirectionality

**BREACH #11** — 1.7 relatedTerms one-way links (not bidirectional)
- **File:field:** `loreDictionary.js` — `relatedTerms`
- **Contradiction:** **58 asymmetric links** among dictionary terms. Concentrated in (a) the 12 dual-origin founders — e.g. `veyra -> {frostwood-reach, frostmaw_holdfast, mimir, shaper}`, `sylvanus -> {frostwood-reach, mimir, shaper}`, `torin -> {cragjaw-peaks, frostmaw_holdfast, groven, shaper}`, `xyris -> {harbinger, sundrift-vale}`, `malakor -> {frozen_archive, harbinger, nordhalla}`, `jax -> {gambit, iceheart-sea, merrowport}`, `orven -> {elias, inquisitor, vreken}` — none of which list the founder back; and (b) class↔location links — `berserker->{harath_vault, sundale}`, `gambit->{iceheart-sea, ironjaw_port, merrowport}`, `warden->{cragjaw-peaks, frozen_archive, frostmaw_holdfast, the_warden}`, `animist->{frozen_archive, nordhalla, sundrift-vale}`.
- **Severity:** P2 · **Fix:** For each A→B, add A to B.relatedTerms (or deliberately drop the link). Highest value: back-link the 12 founders from their regions/locations/classes.

---

## 1.8 — NPC ↔ faction ↔ location triad

> All 20 leader NPCs exist and all their `locationIds` resolve. The gap is **factions**: 20 of the leaders reference **order-faction ids that do not exist** in `factionStore.SEEDED_FACTIONS`. Only `dawn-vigil` and `vat-breakers-guild` (referenced by `sol-kaessen` and `alaric`) already exist; every other order is a missing faction. `bri-vessela` references two missing factions → 21 distinct missing references.

**BREACH #12–#32** — 1.8 Leader NPC factionId not in factionStore (21 references across 20 leaders)
- **File:field:** `npcStore.js` — `<leader>.factionIds`
- **Severity:** P1
- **Missing order-factions:** `bloodhammer-line` (hark), `ancestral-convergence` (sera), `canopy-ledger` (vel-otharen), `frozen-order-of-the-elk` (skadi), `frostmaw-conclave` (fex-vestara), `congregation-of-the-silence` (mor-vereth), `merrowport-house` (merr-cael), `doom-choir` (malakor), `barbed-vow` (vrael), `lunar-communion` + `briaran-groves` (bri-vessela), `covenant-of-the-scar` (sol-kaessen), `tide-choir` (mer-lyrisa), `cultivar` (vespera), `ashen-communion` (sol-vareths), `twice-born` (kor-vasseth), `form-convergence` (veyra), `aegis` (thrak-damos), `distillery` (varis), `the-bound` (alaric), `silent-hunt` (sylas).
- **Fix:** Either create these 19 order-factions in `factionStore.js` (id, name, regionId, classAffinities, leader.npcId) — the recommended path, since Part 3 surfaces them — **or** formally document each as class-internal and strip the `factionIds` entry. This is the single largest body of work in Part 1 and maps 1:1 onto the brief's §1.8 note.

---

## 1.9 — merged-class transition.legacySite references

> Of the 8 `transition.legacySite` sites on concept entries, **only `Sol's Anvil Mesa`** (Titan concept) is referenced in `zoneData.js`. The other 7 are named nowhere in the zone/deep-location data.

**BREACH #33–#39** — 1.9 transition.legacySite not referenced in zone/deep data
- **File:field:** `loreDictionary.js` — `<concept>.transition.legacySite`
- **Severity:** P2
- **Unreferenced sites (→ concept):** `The Old Dance-Floor` (bladedancer) · `The Veil-Speaker Shrine` (deathcaller) · `The Old Foundry` (dreadnaught) · `The Cleansing Chapels` (exorcist) · `The Covenbane Stronghold` (covenbane) · `The Calcifying Vats` (formbender) · `Vesper's Basalt-Phylactery` (lichborne).
- **Fix:** Add each as a `zoneData` entry, a `deepLocationData` subLocation, or a named reference inside an existing location's description (e.g. Covenbane Stronghold + Cleansing Chapels fold into existing Bryngloom Inquisitor seats; Calcifying Vats + Old Foundry fold into Frostmaw/Cragjaw). ✅ `Sol's Anvil Mesa` already resolved (dawn-vigil territory).

---

## 1.10 — frostcirque dangling reference

**BREACH #40** — 1.10 Dangling frostcirque reference
- **File:field:** `loreDictionary.js:642` — `rime_born.relatedTerms='frostcirque'`
- **Contradiction:** `rime_born` lists `'frostcirque'`, but **no `frostcirque` entry exists** in `LORE_DICTIONARY`. Per audit §8.1, Frostcirque was merged into `the_still_crag` (the `the_still_crag` fullEntry already describes Frostcirque as its glacial amphitheater). Note: a pin still exists at `locationCoordinates.js:47` (`'frostcirque'`, ruin, nordhalla) — that geographic reference is now orphaned from the lore dictionary.
- **Severity:** P0
- **Fix:** Rewire `rime_born.relatedTerms` at line 642: replace `'frostcirque'` with `'the_still_crag'`. Then either repoint `locationCoordinates.js:47`'s key to `'the-still-crag'` or keep it as a sub-location pin explicitly parented to the Still Crag.

---

## Priority Table

| Sev | Count (initial → post-fix) | Checks | Items |
|---|---|---|---|
| **P0** | 7 → **0** ✅ | 1.4 (6), 1.10 (1) | #2 #3 #4 #5 #6 #8 #40 — all resolved |
| **P1** | 24 → **22** | 1.3 (1), 1.4 (2), 1.8 (21) | #1 #7 #9 #12–#32 (#7, #9 resolved with the warden id fix) |
| **P2** | 9 → **10** | 1.5 (1), 1.7 (1), 1.9 (7), 1.10 (1 advisory) | #10 #11 #33–#39 + frostcirque advisory |

### P0 fix-order (blockers for Part 2) — ALL DONE ✅
1. ✅ **`wardenData.js:149`** — `id : "Warden"` → `id: "warden"`. Resolved breaches #2, #4, #5, #7, #8, #9 (six, incl. the two P1 tensions).
2. ✅ **`backgroundData.js:396`** — re-pointed `frostChanter → harbinger` to `→ animist` (Skald-valid; lore-coherent with the Frost Chanter's covert preservation of the old animist oral archive). Resolved #3.
3. ✅ **`backgroundData.js:586`** — dropped the `merrowSailor → apex` hook (no Merryn/Myrathil-valid third class exists; gambit + minstrel already covered). Resolved #6.
4. ✅ **`loreDictionary.js:642`** — removed `'frostcirque'` from `rime_born.relatedTerms` (`'the_still_crag'` was already listed). Resolved #40 to P0-clear; the geographic pin at `locationCoordinates.js:47` remains a P2 cleanup.

### Recommended next (P1, before Part 3 UI wiring)
5. Reconcile the Warden leader name string (#1): `wardenData.js:164` 'Chain-Lord Alaric' ↔ `npcStore['alaric']` 'Alaric the Law-Keeper'.
6. Decide the 19 missing order-factions: create them in `factionStore.js` (preferred) or document as class-internal (#12–#32).

### Polish (P2, can run alongside Part 2)
7. Standardize worldFriction generalization tokens (#10).
8. Back-link founders/regions in `relatedTerms` (#11).
9. Anchor the 7 legacy sites in zone/deep data (#33–#39); decide the fate of the `frostcirque` map pin.

---

## P0 Resolution Log

| Breach | File | Edit | Verified |
|---|---|---|---|
| #2 #4 #5 #7 #8 #9 | `classes/wardenData.js:149` | `id : "Warden"` → `id: "warden"` | harness: class list shows `warden`; 1.4 fully clean |
| #3 | `backgroundData.js:396` | frostChanter hook `harbinger` → `animist` (+ rewritten bridge) | harness: 1.4 clean |
| #6 | `backgroundData.js:586` | removed `merrowSailor` `apex` hook | harness: 1.4 clean |
| #40 | `loreDictionary.js:642` | removed `'frostcirque'` from `rime_born.relatedTerms` | harness: P0=0; grep confirms no `frostcirque` in file |

---

*Harness: `lore_audit.js` (loads all modules, runs checks 1.1–1.10, emits `lore_audit_breaches.json`). Re-run after each P0 edit to confirm the breach clears.*
