import React from 'react';

export const CopyIcon = ({ className }) => (
  <i className={`fas fa-copy ${className || ''}`} />
);

export const SaveIcon = ({ className }) => (
  <i className={`fas fa-save ${className || ''}`} />
);

export const DownloadIcon = ({ className }) => (
  <i className={`fas fa-download ${className || ''}`} />
);

export const CheckCircleIcon = ({ className }) => (
  <i className={`fas fa-check-circle ${className || ''}`} />
);

export const XCircleIcon = ({ className }) => (
  <i className={`fas fa-times-circle ${className || ''}`} />
);

export const InfoCircleIcon = ({ className }) => (
  <i className={`fas fa-info-circle ${className || ''}`} />
);

export const AlertTriangleIcon = ({ className }) => (
  <i className={`fas fa-exclamation-triangle ${className || ''}`} />
);

export const ExternalLinkIcon = ({ className }) => (
  <i className={`fas fa-external-link-alt ${className || ''}`} />
);
