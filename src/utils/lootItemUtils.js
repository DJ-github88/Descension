// Simple loot item utilities stub

export const generateLootFromTable = (lootTable) => {
    return []; // Return empty array for now
};

export const calculateItemValue = (item) => {
    return item?.value || 0;
};

export const getItemRarity = (item) => {
    return item?.rarity || 'common';
};

export const processCreatureLoot = (creature) => {
    return []; // Return empty array for now
};

export const processCreaturesLoot = (creatures) => {
    return []; // Return empty array for now
};
