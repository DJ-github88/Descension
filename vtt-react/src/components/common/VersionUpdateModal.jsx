// Version update modal is disabled to prevent disruptive popups.
// The app now uses an always-fresh network-first model that updates automatically on load.
export default function VersionUpdateModal() {
  return null;
}
